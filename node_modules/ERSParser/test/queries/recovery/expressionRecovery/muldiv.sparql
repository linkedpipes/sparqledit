SELECT *  WHERE 
{
    filter (?a * )
    filter (?a * wrong * ?b)
    filter (?a * select * ?b)
    filter (?a + ?b * wrong * ?b)    

    filter (?a / )
    filter (?a / wrong / ?b)
    filter (?a / select / ?b)
    filter (?a + ?b / wrong / ?b)    
}