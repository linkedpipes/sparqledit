SELECT *  WHERE 
{
    filter ((?e) ?wrong)
    filter (avg(?a) ?wrong)
    filter (<http://example.com> ?wrong)
    filter (<http://example.com/function>(?arg) ?wrong)
    filter ("literal" ?wrong)
    filter (?var  ?wrong) 
}