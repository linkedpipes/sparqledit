SELECT * WHERE  
{
    ?a ?b ?c.
    filter (?a
    filter (?a
    GRAPH ?g {
        ?a ?B ?c
    }
    filter (?a
    union {
        ?a ?b ?c
    }
    filter (?a
    minus {
        ?a ?b ?c
    }
    bind ( ?a as ?b
    bind (?a as ?b
    optional {
        ?a ?b ?c
    } 
    filter (?a 
    {
        ?a ?b ?c
    }
}