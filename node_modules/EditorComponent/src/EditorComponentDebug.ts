import { BaseEditorComponent } from "./BaseEditorComponent";
import { IPluginManagerFactory } from './factory/plugin/PluginManagerFactory';
import { DebugPluginManagerFactory } from "./factory/plugin/DebugPluginManagerFactory";

/**
 * Debug version of sparql editor.
 */
export class EditorComponentDebug extends BaseEditorComponent {
    constructor(public container: HTMLElement) {
        super(container, new DebugPluginManagerFactory())
    }
}