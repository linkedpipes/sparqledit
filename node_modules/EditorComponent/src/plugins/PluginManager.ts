import { IPlugin, IEditorPluginApi } from './IPlugin';

export class PluginManager {
    private plugins: IPlugin[] = [];

    registerPlugin(plugin: IPlugin) {
        this.plugins.push(plugin);
    }

    initPlugins(monacoEditor: monaco.editor.IStandaloneCodeEditor, editorPluginApi: IEditorPluginApi) {
        this.plugins.forEach(x => x.init(monacoEditor, editorPluginApi));
    }
}