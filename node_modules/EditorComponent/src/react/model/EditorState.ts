import { IEditorSettings } from './EditorSettings';
import { ListItem } from "../ListComponent";
import * as immutable from "immutable";
import { TypedRecord, makeTypedFactory } from "EditorComponent/node_modules/typed-immutable-record/dist";

export interface IEditorState {
    settings: IEditorSettings;
}

var defaultEditorState: IEditorState = { settings: null };

export interface IEditorStateRecord extends TypedRecord<IEditorStateRecord>, IEditorState { }

export const EditorStateFactory = makeTypedFactory<IEditorState, IEditorStateRecord>(defaultEditorState);