import { EditorSettingsStore } from '../../store/EditorSettingsStore';
import * as React from "react";
import { observer } from "mobx-react";

export interface JsonFormattterProps {
    objectToJson: any
}

export class JsonFormatter extends React.Component<JsonFormattterProps, any> {
    render() {
        return <pre>{JSON.stringify(this.props.objectToJson, null, 4)}</pre>
    }
}