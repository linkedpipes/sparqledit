import { observable } from "mobx"

export enum ToolType {
    SideBar,
    Bottom
}

export class Tool {
    constructor(public content: JSX.Element, public type = ToolType.SideBar) {

    }
}

export class ToolStore {
    @observable tools: Tool[] = [];
}