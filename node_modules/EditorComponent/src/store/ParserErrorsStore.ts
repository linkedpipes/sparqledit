import { ParserError } from "ERSParser"
import { observable } from "mobx";

export class ParserErrorStore {
    @observable errors: string[] = [];
    @observable containsHaltingError: boolean = false;
};