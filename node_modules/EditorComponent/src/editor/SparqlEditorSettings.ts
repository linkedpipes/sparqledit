export interface SparqlEditorSettings {
    monacoEditorUrl: string;

    ontologySourceUrl: string;

    ontologySourceType?: "custom" | "raw" | "precomputed";

    defaultEditorValue?: string;

    saveEditorValueInCookies?: boolean

    propertySelectorType?: "Restricted" | "Full";

    isLoggingEnabled?: boolean;

    logOntologyBuildError?: boolean;
}