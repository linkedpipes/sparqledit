import { CommanderWrapper } from "EditorTool/src/Commander/CommanderWrapper";
import { PrecomputeCommand } from "EditorTool/src/Commands/PrecomputeCommand";

var precomputeCommand = new PrecomputeCommand();
var commanderWrapper = new CommanderWrapper([precomputeCommand]);
commanderWrapper.run(process.argv);