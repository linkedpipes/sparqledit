import { IOntologyClass } from '../../Ontology/OntologyClass';
import { OntologyConcept } from '../../Ontology/OntologyHierarchy';

export interface IPropertySelector {
    getPropertiesFor(ontologyClass: IOntologyClass): OntologyConcept[];
}